# TurboBoxOS 系统规范

## 1. 项目定位

Turbox OS 是一个面向操作系统教学场景的微型内核。核心目标不是做出一个可用的系统，而是让学习者能从头到尾读懂一个 OS 的最小闭环。

设计原则：

- 每一层只保留最小实现，能跑就行，不求完善
- 代码可读性优先于性能和功能
- 正确性优先于优雅
- 刻意保持单线程，避免并发复杂度
- 刻意无网络、无磁盘，聚焦核心概念
- 所有模块自包含，不依赖任何第三方代码

---

## 2. 系统架构

### 2.1 分层模型

```
+--------------------------------------------------+
|                  user 用户程序层                  |
|              shell / demo                         |
+--------------------------------------------------+
|                syscall 系统调用层                 |
|         turbox_api.h / syscall.h                  |
+--------------------------------------------------+
|              kernel 微内核核心层                  |
|      进程 / IPC / 调度 / 系统调用分发              |
+--------------------------------------------------+
|        middle 中间层 / fs 文件系统层              |
|       VFS 适配 / IPC 服务 / TurboxFS              |
+--------------------------------------------------+
|           drv 驱动层 / hal 硬件抽象层             |
|         VGA / 键盘 / PIT / GDT / IDT              |
+--------------------------------------------------+
|                   硬件                             |
|        x86 32位 / 端口 IO / 物理内存               |
+--------------------------------------------------+
```

### 2.2 层次职责

| 层 | 职责 | 关键模块 |
|----|------|----------|
| boot | 实模式引导、磁盘读取、进入保护模式 | boot.asm / loader.asm |
| hw | 端口 IO、GDT、IDT 等硬件基础 | ports.h / gdt.cpp / idt.cpp |
| hal | 聚合硬件初始化，屏蔽实现细节 | hal.h / hal.cpp |
| drv | 具体设备驱动 | vga.cpp / keyboard.cpp |
| mm | 物理页管理、内核堆分配 | pmm.cpp / heap.cpp |
| kernel | 进程、IPC、调度、系统调用 | kernel.cpp / fork.cpp / ipc.cpp / sched.cpp |
| fs | 内存文件系统 | turboxfs.cpp |
| middle | VFS 和 IPC 适配 | vfs.cpp / ipc_adapter.cpp |
| syscall | 用户态系统调用 API | syscall.cpp / turbox_api.h |
| user | 用户程序 | shell.cpp / demo.cpp |
| runtime | 自研基础函数 | string.cpp |
| tools | 构建、打包、桥接 | build.py / pack_image.py / bridge.py |
| web | 终端模拟 | index.html / terminal.js |

### 2.3 启动流程

```
BIOS 加载 boot.asm 到 0x7C00
  → boot.asm 读取 loader 和内核到 0x10000
    → loader 开启 A20、加载 GDT、进入保护模式
      → 跳转 kernel_main
        → hal_init 初始化硬件
        → pmm_init 初始化物理内存
        → procs_init 初始化进程表
        → fork FS 服务进程
        → fork shell 进程
        → 进入 idle 循环
```

---

## 3. 内存布局

### 3.1 启动内存分布

| 地址范围 | 用途 |
|----------|------|
| 0x7C00 | BIOS 加载启动扇区的位置，栈设置在扇区下方 |
| 0x10000 | loader 和内核的加载地址，链接基址与此对齐 |
| 0x90000 | 内核栈指针位置 |
| 0xB8000 | VGA 文本模式显存 |
| 0-1MiB | 低内存区，包含 BIOS 数据、显存、内核装载区 |

### 3.2 物理内存管理

- 位图管理，每页 4KiB
- 前 1MiB = 256 页固定保留
- 最大管理 64MiB = 16384 页
- 位图大小：2048 字节静态数组
- 分配策略：从上次位置开始环形扫描
- 释放时检查页对齐、越界、双重释放

### 3.3 内核堆

- 基于 pmm 整页扩张
- 显式空闲链表，首次适配
- 块头包含大小、空闲标志、next 指针
- 分裂阈值：剩余空间 ≥ 块头 + 16 字节才分裂
- 释放时合并物理相邻空闲块

---

## 4. 进程模型

### 4.1 PCB 结构

| 字段 | 说明 |
|------|------|
| pid | 进程号 = 槽位下标 + 1 |
| state | FREE / READY / BLOCKED / ZOMBIE |
| entry | 入口函数指针 |
| ipc_msg | 最近收到的消息内容 |
| ipc_from | 消息发送者 pid |
| ipc_pending | 信箱是否有未读消息 |

### 4.2 进程表

- 静态数组，容量 16
- 槽位 0 为内核主上下文，pid 1
- FS 服务进程稳定占用 pid 2
- pid 分配从槽位 1 开始环形查找

### 4.3 协作式语义

- 没有抢占，没有栈切换
- 进程实际是"可恢复执行点"
- 进程主动让出或 IPC 阻塞时才切换
- 入口返回视作本轮执行结束

---

## 5. 调度器

### 5.1 策略

- 简单轮转
- 从当前进程下一个槽位开始环形扫描
- 跳过内核主上下文和阻塞进程
- 无其他就绪进程时立即返回

### 5.2 特点

- 没有时间片
- 没有优先级
- 没有上下文栈
- 正确性依赖进程自觉让出

---

## 6. IPC

### 6.1 消息模型

- 载荷：单个 `int`
- 信箱：每进程单字容量
- 未读消息存在时拒收
- 发送方填满后让出等待
- 接收方空箱时让出等待

### 6.2 自旋上限

- `IPC_SPIN_LIMIT = 1024`
- 防止对端进程已死导致永久等待
- 超时按失败处理

### 6.3 FS 服务消息协议

| 字段 | 说明 |
|------|------|
| 操作码 | 低 8 位，目前只有 `IPC_OP_RUN = 0x01` |
| fd | 高 24 位，文件句柄 |
| 应答 | 0 成功，非 0 失败 |

---

## 7. 文件系统

### 7.1 容量

| 项 | 值 |
|----|----|
| 最大文件数 | 64 |
| 文件名长度 | 32 字节 |
| 单文件最大 | 4096 字节 |

### 7.2 接口

- `tfs_init()` 初始化
- `tfs_create(name)` 创建文件，幂等
- `tfs_write(fd, data, len)` 追加写入，截断返回
- `tfs_read(fd, buf, len)` 从头读取
- `tfs_run(name)` 解释运行
- `tfs_list()` 列出所有文件

### 7.3 mini 脚本命令集

| 命令 | 说明 |
|------|------|
| `print <text>` | 输出文本并换行 |
| `putc <char>` | 输出单个字符，不换行 |
| `exit` | 终止脚本 |
| `#` 开头 | 注释 |
| 空行 | 跳过 |

### 7.4 IPC 委托路径

用户态 `vfs_run` → 解析 fd → IPC 发送给 FS 服务 → FS 服务执行 `tfs_run_fd` → 应答。  
IPC 不可用时回退为本地直接调用，保证分阶段集成可用。

---

## 8. 系统调用

### 8.1 调用号

| 编号 | 名称 | 功能 |
|------|------|------|
| 1 | SYS_FORK | 创建进程 |
| 2 | SYS_EXIT | 退出进程 |
| 3 | SYS_CREATE | 创建文件 |
| 4 | SYS_WRITE | 写文件 |
| 5 | SYS_READ | 读文件 |
| 6 | SYS_RUN | 运行文件 |
| 7 | SYS_PUTC | 输出字符 |
| 8 | SYS_GETC | 读取键盘字符 |

### 8.2 陷入约定

- `int 0x80` 触发
- `eax` = 系统调用号
- `ebx` = 参数1
- `ecx` = 参数2
- `edx` = 参数3
- 返回值存入 `eax`
- `sys_call` 使用 `volatile` 防止优化

### 8.3 用户 API

| API | 说明 |
|-----|------|
| `tbx_fork(entry)` | 创建进程，返回 pid |
| `tbx_exit(code)` | 退出进程，不返回 |
| `tbx_create(name)` | 创建文件，返回 fd |
| `tbx_write(fd, data, len)` | 写文件 |
| `tbx_read(fd, buf, len)` | 读文件 |
| `tbx_run(name)` | 运行文件 |
| `tbx_putc(c)` | 输出字符 |
| `tbx_getc()` | 读键盘字符 |

---

## 9. 驱动规范

### 9.1 VGA

- 80x25 文本模式
- 显存地址 0xB8000
- 属性字节 0x07：黑底浅灰字
- 支持换行、退格、自动折行、滚屏
- 退格不清字符，仅移动光标

### 9.2 键盘

- PS/2 控制器，端口 0x60 数据口，0x64 状态口
- 扫描码集 1
- 轮询方式，非中断
- 支持小写字母、数字、空格、回车、退格
- 断码被忽略，只处理按下事件

### 9.3 PIT 定时器

- 通道 0，方波发生器
- 100Hz 节拍
- 分频值 = 1193182 / hz
- `hal_ticks()` 返回 tick 计数

---

## 10. 构建规范

### 10.1 工具链

- NASM：`-f elf32`
- C++：`-std=c++17 -ffreestanding -nostdlib -fno-exceptions -fno-rtti -O2`
- C：`-std=c11 -ffreestanding -nostdlib -O2`
- 链接：`-T linker.ld`，输出平坦二进制

### 10.2 镜像格式

```
[0..5]    魔数 "TBXIMG"
[6..9]    文件数 N（小端 uint32）
[N * 40]  文件表，每项 40 字节：
           名称 32 字节（NUL 填充）
           偏移 4 字节（相对镜像头）
           长度 4 字节
[data]    依次拼接文件数据
```

### 10.3 构建流程

```
build.py 生成 turbox.bin
  → pack_image.py 打包 turbox.img
    → bridge.py 解析镜像或生成演示状态
```

---

## 11. Web 演示

### 11.1 终端模拟

- 纯 HTML/CSS/JS
- 无第三方依赖
- 命令集：create/write/run/ls/cat/help
- 支持历史命令 ↑/↓
- 回显命令、错误着色

### 11.2 双模式

| 模式 | 触发 | 说明 |
|------|------|------|
| 镜像同步 | fetch 到 fs_state.json | 同步 bridge.py 输出的文件系统状态 |
| 内置模拟 | fetch 失败 | 使用浏览器内存的虚拟文件系统 |

---

## 12. 代码规范

### 12.1 文件头注释

每个文件必须包含：

- 文件名、所属层、项目内绝对路径
- 模块作用、使用者、项目角色
- 引入说明、维护记录

### 12.2 命名约定

- 宏：全大写，下划线分隔
- 函数：小写加下划线
- 结构体：大驼峰
- 全局变量：`g_` 前缀
- 静态函数：模块名前缀，如 `tfs_`、`sh_`、`kbd_`

### 12.3 注释原则

- 解释为什么，不重复做什么
- 公共接口写清楚，私有细节少废话
- 不用 AI 模板腔
- 注释不超过代码量

---

## 13. 已知限制

| 限制 | 原因 |
|------|------|
| 单线程 | 教学取舍 |
| 无抢占 | 教学取舍 |
| 无地址空间隔离 | 教学取舍 |
| 无磁盘文件系统 | 教学取舍 |
| 无网络 | 教学取舍 |
| 内存上限 64MiB | 位图静态数组 |
| 文件表 64 文件 | 静态文件表 |
| 单文件 4KiB | 内联存储 |
| 无 MSVC 支持 | GCC/Clang 交叉编译 |

---

## 14. 路线图

### 短期

- [ ] 支持更多系统调用
- [ ] 文件系统支持删除和重命名
- [ ] 支持大写字母输入

### 中期

- [ ] IDT 安装真正的异常处理
- [ ] 进程支持阻塞队列
- [ ] VGA 支持光标移动控制

### 长期

- [ ] 用户态/内核态地址空间隔离
- [ ] 抢占式调度
- [ ] 磁盘文件系统
- [ ] 虚拟内存